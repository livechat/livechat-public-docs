---
weight: 30
category: "guides"
subcategory: "installing-livechat"
title: "LiveChat for Android"
subtitle: "Installation and Setup"
tagline: "Embed LiveChat in your Android app."
desc: "Learn how to embed LiveChat in your Android app. Let your users contact agents directly from the mobile application."
---

# Introduction

Add real-time customer support to your Android application with LiveChat's native SDK. This guide covers installation, setup, and advanced features.

<Warning>
💡 The SDK is now Kotlin-based and uses the new package `com.livechatinc.chatsdk`. See migration notes if you are upgrading from v2.x.x.
</Warning>

# Requirements

- Android 5.0 (API level 21) or higher
- Java 8 or higher

# Installation

## 1. Add the JitPack repository

Add JitPack to your root `build.gradle`:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
allprojects {
    repositories {
        maven { url 'https://jitpack.io' }
    }
}
```
</TabItem>
<TabItem value="java">

```java
allprojects {
    repositories {
        maven { url 'https://jitpack.io' }
    }
}
```
</TabItem>
</Tabs>

## 2. Add the dependency

Add to your app's `build.gradle`:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
dependencies {
    implementation 'com.github.livechat:chat-window-android:3.0.0'
}
```
</TabItem>
<TabItem value="java">

```java
dependencies {
    implementation 'com.github.livechat:chat-window-android:3.0.0'
}
```
</TabItem>
</Tabs>

## 3. Add Internet permission

Add to your `AndroidManifest.xml`:

```xml
<uses-permission android:name="android.permission.INTERNET" />
```

# Usage

## Initialize the SDK

Initialize in your `Application`'s `onCreate()`:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.initialize("<LICENSE>", this)
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.initialize("<LICENSE>", this);
```
</TabItem>
</Tabs>

## Show the Chat

Show the chat to a customer:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().show()
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.getInstance().show();
```
</TabItem>
</Tabs>

# Customer Information

Pre-fill the pre-chat form and provide customer details:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().setCustomerInfo(
    "Joe", // Name
    "joe@mail.com", // Email
    "0", // Group ID, defaults to "0"
    Collections.singletonMap("internalCustomerId", "ABC123") // Custom params
)
```
</TabItem>
<TabItem value="java">

```java
Map<String, String> customParams = new HashMap<>();
customParams.put("internalCustomerId", "ABC123");
LiveChat.INSTANCE.getInstance().setCustomerInfo(
    "Joe", // Name
    "joe@mail.com", // Email
    "0", // Group ID
    customParams // Custom params
);
```
</TabItem>
</Tabs>

> **Note:** Call this before `LiveChat.show()`.

# Unread Message Counter

React to new messages using a listener:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().setNewMessageListener {
    // Handle new message
}
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.getInstance().setNewMessageListener(() -> {
    // Handle new message
    return null;
});
```
</TabItem>
</Tabs>

# UI Customization

## Localizing Text

Override string resources in your app's `strings.xml`:

```xml
<string name="failed_to_load_chat">Couldn\'t load chat.</string>
<string name="cant_share_files">File sharing is not configured for this app</string>
<string name="reload_chat">Reload</string>
```

## Custom Error View

Override `live_chat_error_layout.xml` in your layout resources.
Your custom view must contain a `View` with `live_chat_error_button` id.

## Customizing Activity

The chat activity follows your app's theme.
Override the activity declaration in your `AndroidManifest.xml` if needed.

# Clearing Chat Session

Clear the chat session and cookies:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.clearSession()
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.clearSession();
```
</TabItem>
</Tabs>

# Handling Links

Intercept links sent in chat:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().setUrlHandler(object : UrlHandler {
    override fun handleUrl(uri: Uri): Boolean {
        // Return true if handled, false to open in browser
        return false
    }
})
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.getInstance().setUrlHandler(new UrlHandler() {
    @Override
    public boolean handleUrl(Uri uri) {
        // Return true if handled, false to open in browser
        return false;
    }
});
```
</TabItem>
</Tabs>

# Troubleshooting

## React to Errors

Set an error listener:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().setErrorListener { error ->
    // Handle the error
}
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.getInstance().setErrorListener(error -> {
    // Handle the error
    return null;
});
```
</TabItem>
</Tabs>

## Logger

Set logging level before initializing LiveChat:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
Logger.setLogLevel(Logger.LogLevel.VERBOSE)
```
</TabItem>
<TabItem value="java">

```java
Logger.setLogLevel(Logger.LogLevel.VERBOSE);
```
</TabItem>
</Tabs>

## File Picker Activity Not Found

React to missing file picker activity:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.getInstance().setFileChooserNotFoundListener {
    Log.e("Example", "File chooser not found. Please check your configuration.")
}
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.getInstance().setFileChooserNotFoundListener(() -> {
    Log.e("Example", "File chooser not found. Please check your configuration.");
    return null;
});
```
</TabItem>
</Tabs>

# Advanced Usage

## LiveChatView Lifecycle Modes

Control the lifecycle of the chat view:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
LiveChat.initialize("<LICENSE>", this, LiveChatViewLifecycleMode.WHEN_SHOWN)
```
</TabItem>
<TabItem value="java">

```java
LiveChat.INSTANCE.initialize("<LICENSE>", this, LiveChatViewLifecycleMode.WHEN_SHOWN);
```
</TabItem>
</Tabs>

## Embedding LiveChatView

Add to your layout XML:

```xml
<com.livechatinc.chatsdk.LiveChatView
    android:id="@+id/live_chat_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"/>
```

Attach to activity or fragment:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
liveChatView.attachTo(this)
```
</TabItem>
<TabItem value="java">

```java
liveChatView.attachTo(this);
```
</TabItem>
</Tabs>

Initialize with a listener:

<Tabs
  groupId="language"
  defaultValue="kotlin"
  values={[
    {label: 'Kotlin', value: 'kotlin'},
    {label: 'Java', value: 'java'},
  ]}>
<TabItem value="kotlin">

```kotlin
liveChatView.init(initCallbackListener)
```
</TabItem>
<TabItem value="java">

```java
liveChatView.init(initCallbackListener);
```
</TabItem>
</Tabs>

# Migrating from v2.5.0 to 3.0.0

- SDK is now Kotlin-based
- API is streamlined and uses `LiveChat` singleton
- Package changed to `com.livechatinc.chatsdk`
- See [README](https://github.com/livechat/chat-window-android) for full migration steps

# Localization

Override string resources in your app's `strings.xml`:

```xml
<string name="failed_to_load_chat">Couldn\'t load chat.</string>
<string name="cant_share_files">File sharing is not configured for this app</string>
<string name="reload_chat">Reload</string>
```
